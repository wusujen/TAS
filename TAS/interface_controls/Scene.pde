/*==============  Scene  =============*
 A simple Class that manages the creation and
 storage of Scenes, associated with Scene Elements.
 *=========================================*/
class Scene {
  String sceneName;
  
  Scene(String objName) {
    sceneName = objName;
  }
  
  void setup() {
    // count the number of scene nodes in the XML file
    // grab their file names and populate the global Scene Array
    
    // if there is nothing, then create a scene tab with a filename of "Scene 1"

  }
  
}
